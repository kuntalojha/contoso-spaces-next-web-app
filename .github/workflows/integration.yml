name: Bulid Next.js web application
on: push
jobs:
  build-project:
    name: Bulid project
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Install npm dependencies
      run: npm install
    - name: Build project assets
      run: npm run build
